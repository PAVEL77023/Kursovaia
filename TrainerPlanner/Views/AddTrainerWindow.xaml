<Window x:Class="TrainerPlanner.Views.AddTrainerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавить тренера" Height="250" Width="400"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Новый тренер" FontWeight="Bold" FontSize="16" Grid.Row="0" Margin="0,0,0,15"/>

        <TextBlock Text="ФИО:" Grid.Row="1" Margin="0,0,0,5"/>
        <TextBox x:Name="TbFullName" Grid.Row="2" Height="30" Margin="0,0,0,10"/>

        <TextBlock Text="Специализация:" Grid.Row="3" Margin="0,0,0,5"/>
        <TextBox x:Name="TbSpecialization" Grid.Row="4" Height="30"/>

        <Button Content="Сохранить" Click="BtnSave_Click" Width="100" Height="30" 
                Margin="0,15,0,0" Background="#FF4CAF50" Foreground="White"/>
    </Grid>
</Window>
